import type React from "react"
export const metadata = {
  title: "Confirmação de Pagamento",
  description: "Confirmação do seu pagamento",
}

export default function ConfirmacaoLayout({ children }: { children: React.ReactNode }) {
  return children
}
